#include <stdio.h>
/**
*main - Entry point
*Discription: 'Check for numbers if it is positive or negative'
*Return: always 0
*/
int main(void)
{
int l;
int x;

for (l = 48; l <= 57; l++)
{
	putchar(l);
}
for (x = 97; x <= 102; x++)
{
	putchar(x);
}
putchar('\n');
return (0);
}
